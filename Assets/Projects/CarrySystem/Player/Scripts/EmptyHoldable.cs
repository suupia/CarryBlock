using Carry.CarrySystem.Player.Interfaces;
#nullable enable
namespace Carry.CarrySystem.Player.Scripts
{
    public class EmptyHoldable : IHoldable
    {
        // MonoのPresenterの方にアタッチされているViewオブジェクトが一つで、
        // EnableHoldableView(IHoldable holdable)に渡す具象クラスがないときにつかう
        
    }
}